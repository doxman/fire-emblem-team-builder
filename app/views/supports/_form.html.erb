<%= form_with(model: [ @character, @character.supports.build ]) do |form| %>
  <p>
    <%= form.label :partner_name %><br>
    <%= form.select :partner_name, availableSupports(@character) %>
  </p>
  <p>
    <%= form.submit options={ disabled: shouldSubmitBeDisabled?(@character) } %>
  </p>
<% end %>
