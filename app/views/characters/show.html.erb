<p>
  <strong>Name:</strong>
  <%= @character.name %>
</p>
<p>
  <strong>Recruitment chapter:</strong>
  <%= @character.recruitment_chapter %>
</p>

<h2>Supports</h2>
<%= render @character.supports %>

<h2>Add a support:</h2>
<%= render 'supports/form' %>

<%= link_to 'Edit', edit_game_character_path(@game, @character) %> |
<%= link_to 'Back', game_path(@game) %>
